spring:
  datasource:
    url: jdbc:h2:mem:testdb;
    driverClassName: org.h2.Driver
    username: admin
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
      server:properties:
        webAllowOthers: true
  security:
    user:
      name: admin
      password: admin
  # spring:
  # jwt:
  #   secret: aazfgsdfhjtfurdtgcxb
  #   expiration: 3600
